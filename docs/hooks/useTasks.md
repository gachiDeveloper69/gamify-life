# 🗂️ useTasks Hook

## Назначение

Хук для работы с задачами (создание, удаление, изменение статуса). Также может возвращать текущие задачи/статистику для отображения в UI.

## API

```typescript
const { createTask, deleteTask, updateScore, markCompleted, markIncomplete, updateTask } =
  useTasks();
```

## Возвращаемое значение

- `tasks` - массив задач для отображения
- `stats` - статистика для отображения

- объект с функциями для работы с задачами:

### createTask(taskData: CreateTaskData)

Создает новую задачу

**Параметры:**

- `taskData` - данные задачи (тип CreateTaskData)

### deleteTask(id: string)

Удаляет задачу по ID

**Параметры:**

- `id` - уникальный идентификатор задачи

### updateScore(points: number, decrease = false)

Удаляет задачу по ID

**Параметры:**

- `points` - количество очков, которое будет добавлено/вычтено
- `decrease` - передаем true если хотим вычесть а не прибавить

### markCompleted(id: string)

Проставляет в задаче отметку о выполнении, время закрытия задачи (текущая дата) и начисляет очки XP в зависимости от категории задачи (или установленного пользователем кол-ва очков за задачу)

**Параметры:**

- `id` - уникальный идентификатор задачи

### markIncomplete(id: string)

Снимает в задаче отметку о выполнении, сбрасывает время закрытия задачи и списывает очки XP в зависимости от категории задачи (или установленного пользователем кол-ва очков за задачу)

**Параметры:**

- `id` - уникальный идентификатор задачи

### updateTask(id: string, updates: Partial<Omit<Task, 'id' | 'createdAt' | 'completed' | 'completedAt'>>)

Позволяет изменить остальные свойства задачи, такие как название, описание, дедлайн и др.

**Параметры:**

- `id` - уникальный идентификатор задачи
- `updateTask` - параметры задачи которые надо изменить

## Обработка ошибок

Логирование в консоль(при необходимости) + проброс дальше:

```typescript
catch (error) {
  console.log(`Error adding task "${title}":`, error);
  throw error;
}
```

## Особенности реализации

- всегда работает с актуальными значениями благодаря хуку useLocalStorage
- функцию `updateScore`- вне хука использовать не рекомендуется. Нужна для работы с `markCompleted`/`markIncomplete`

## Примеры кода

Добавим когда появятся :)

## Типы данных

Ссылки на типы:

- [Task](../../src/types/task.ts)
- [TaskStats](../../src/types/task.ts)
- [CreateTaskData](../../src/types/task.ts)

## Вспомогательные утилиты

Хук использует внутренние утилиты для валидации и обработки задач:

- `validate()` - проверка данных задачи
- `setUpdates()` - подготовка обновлений

[Исходный код утилит](./../../src/utils/tasks/tasksUtils.ts)
