# 🗂️ useLocalStorage Hook

## Назначение

Хук для синхронизации состояния React с localStorage. Достаёт/записывает данные в localStorage.

## API

```typescript
const [value, setValue] = useLocalStorage<T>(key, initialValue);
```

## Параметры

`key`: string - уникальный ключ для localStorage
`initialValue`: T - начальное значение

## Возвращаемое значение

Кортеж `[value, setValue]` аналогично `useState`

## Особенности реализации

### Инициализация

- При монтировании хук пытается прочитать данные из localStorage
- При ошибке парсинга используется initialValue
- Ошибки логируются в консоль

## Сохранение

- Автоматически сохраняет в localStorage при изменении
- Поддерживает функциональные обновления
- Обрабатывает ошибки записи

## Примеры использования

### Получение из хранилища или запись новых задач

```typescript
const [tasks, setTasks] = useLocalStorage<Task[]>('tasks', []);
const [stats, setStats] = useLocalStorage<TaskStats>('task-stats', {
  totalPoints: 0,
  dailyPoints: 0,
  lastReset: new Date().toISOString().split('T')[0], // Дата последнего сброса daily points
});
```
